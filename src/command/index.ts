import { LiteralArgumentBuilder, RequiredArgumentBuilder } from "./builder";

const literal = LiteralArgumentBuilder.literal;
const argument = RequiredArgumentBuilder.argument;

export { literal, argument };
